{
  "name": "xuanyuku-cli",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter @xuanyuku/cli build --watch"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}