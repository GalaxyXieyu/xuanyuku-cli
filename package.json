{
  "name": "xuanyuku-cli",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "pack:cli": "node scripts/pack-cli.mjs",
    "dev": "pnpm --filter @xuanyuku/cli build --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.7.0"
  }
}